=========================================================================================
                    DOCKER CONTAINER RESTART POLICIES
=========================================================================================


=========================================================================================
# 0. WHAT IS RESTART POLICY?
=========================================================================================

Restart policy controls:

When a stopped/crashed container should restart automatically.

Useful for reliability.


=========================================================================================
# 1. WHY USE IT?
=========================================================================================

If container crashes,

restart policy can auto restart it.

Useful in:

- APIs

- Databases

- Services


=========================================================================================
# 2. SET RESTART POLICY
=========================================================================================

Use:

docker run --restart POLICY IMAGE


Example:

docker run --restart always nginx


=========================================================================================
# 3. TYPES OF RESTART POLICIES
=========================================================================================

---------------------------------------------------------
no
---------------------------------------------------------

Default.

No automatic restart.


Example:

--restart no


---------------------------------------------------------
always
---------------------------------------------------------

Always restart container
if it stops.

Example:

--restart always

---------------------------------------------------------
unless-stopped
---------------------------------------------------------

Restart always

EXCEPT

if you manually stopped it.


Example:

--restart unless-stopped


---------------------------------------------------------
on-failure
---------------------------------------------------------

Restart only if container exits with error.

Example:

--restart on-failure


Can set retry count:

--restart on-failure:5


=========================================================================================
# 4. COMMONLY USED
=========================================================================================

Very common:

--restart unless-stopped

Often preferred.


=========================================================================================
# 5. CHECK RESTART POLICY
=========================================================================================

Inspect container:

docker inspect CONTAINER_ID


Check restart policy in metadata.


=========================================================================================
# 6. EXAMPLE
=========================================================================================

Run nginx with auto restart:

docker run -d --restart unless-stopped nginx


=========================================================================================
# 7. INTERVIEW QUESTION
=========================================================================================

Q: What is Docker restart policy?

Answer:

Docker restart policy defines when a container should automatically restart,
such as always, unless-stopped, or only on failure.


=========================================================================================
                                     THE END
=========================================================================================